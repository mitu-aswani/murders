We analyze US gun data collected by the FBI

download-data.r > downloads csv file to data directory

wrangle-data.R > creates a derived dataset and saves as R object in the R directory

analysis.R > generates a plot and saves it in the figs directory
